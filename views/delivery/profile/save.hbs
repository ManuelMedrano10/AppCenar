<div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">
        <div class="card">
            <div class="card-header bg-primary text-light">
                <h5 class="text-center"><i class="bi bi-person-add"></i> Edit Delivery</h5>
            </div>
            <div class="card-body">
                <form enctype="multipart/form-data" action="/delivery-profile/edit" method="POST">
                    <div class="mb-3">
                        <label for="name-input" class="form-label">Name: <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" value="{{delivery.name}}" id="name-input" name="Name"
                            required="required" />
                    </div>
                    <div class="mb-3">
                        <label for="lastname-input" class="form-label">Lastname: <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" value="{{delivery.lastname}}" id="lastname-input" name="Lastname"
                            required="required" />
                    </div>
                    <div class="mb-3">
                        <label for="phone-input" class="form-label">Phone: <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" value="{{delivery.phone}}" id="phone-input" name="Phone"
                            required="required" />
                    </div>
                    <div class="mb-3">
                        <label for="photo-input" class="form-label">Photo of client:</label>
                        <input type="file" class="form-control" value="{{delivery.photo}}" id="photo-input"
                            name="Photo" />
                    </div>
                    <input type="hidden" name="DeliveryId" value="{{delivery._id}}" />
                    <div>
                        <button type="submit" class="btn btn-primary float-end"><i class="bi bi-floppy"></i>
                            Save</button>
                        <a href="/delivery-profile/index" class="btn btn-warning float-end me-2"><i class="bi bi-arrow-left"></i> Back</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>